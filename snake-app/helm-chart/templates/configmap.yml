{{- if .Values.config.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "snake-app.fullname" . }}-config
  labels:
    {{- include "snake-app.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config.data }}
  {{ $key }}: "{{ $value }}"
  {{- end }}
{{- end }}
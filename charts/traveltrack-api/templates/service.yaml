apiVersion: v1
kind: Service
metadata:
  name: {{ include "traveltrack.fullname" . }}
  labels:
    {{- include "traveltrack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "traveltrack.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http

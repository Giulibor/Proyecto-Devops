apiVersion: apps/v1
kind: Deployment
metadata:
  name: snake-app-green
  labels: { app: snake-app, color: green }
spec:
  replicas: 1
  selector: { matchLabels: { app: snake-app, color: green } }
  template:
    metadata: { labels: { app: snake-app, color: green } }
    spec:
      containers:
        - name: snake-app
          image: snake-app:v2
          ports: [ { containerPort: 80 } ]
          livenessProbe:  { httpGet: { path: "/", port: 80 }, initialDelaySeconds: 5,  periodSeconds: 10 }
          readinessProbe: { httpGet: { path: "/", port: 80 }, initialDelaySeconds: 3,  periodSeconds: 5  }

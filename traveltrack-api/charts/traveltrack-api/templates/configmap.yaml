apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ printf "%s-config" (include "traveltrack.fullname" .) | quote }}
  labels:
    {{ include "traveltrack.labels" . | nindent 4 }}
data:
  APP_VERSION: {{ .Values.config.appVersion | quote }}
  PORT: {{ .Values.config.port | quote }}
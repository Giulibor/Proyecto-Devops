apiVersion: v1
kind: Pod
metadata:
  name: bad-pod
  namespace: traveltrack
spec:
  containers:
    - name: c
      image: nginx:latest
      securityContext:
        runAsNonRoot: true
      # sin resources y sin runAsNonRoot → debe ser rechazado